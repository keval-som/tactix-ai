
def generate_post_call_report(full_transcript: str):
    """Produce a summary or report after a call using the transcript.

    Args:
        full_transcript: concatenated user-agent conversation.

    Returns:
        A string report. (stub implementation)
    """
    # placeholder logic
    num_words = len(full_transcript.split())
    return f"Call ended. Transcript contained {num_words} words."

